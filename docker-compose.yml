# Docker Compose configuration for PR-Tracker
# Note: With SQLite, no external database containers are needed
# The SQLite database file (dev.db) will be created locally

version: '3.8'

# No services needed for SQLite development
# Backend runs directly with: pnpm dev
# Database is a local file: ./backend/dev.db

# If you need other services in the future, add them here