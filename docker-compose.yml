# Docker Compose configuration for PR-Tracker
# Note: Using external PostgreSQL (Neon), no local database containers needed
# Backend connects to cloud PostgreSQL database

version: '3.8'

# No services needed for cloud PostgreSQL development
# Backend runs directly with: pnpm dev
# Database is hosted on Neon cloud

# If you need other services in the future, add them here